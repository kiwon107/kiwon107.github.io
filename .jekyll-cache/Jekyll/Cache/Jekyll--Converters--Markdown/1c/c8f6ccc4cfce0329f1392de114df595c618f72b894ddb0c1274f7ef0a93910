I"�<p>본 포스팅은 “윤성우의 열혈 파이썬 중급편” 책 내용을 기반으로 작성되었습니다.
잘못된 내용이 있을 경우 지적해 주시면 감사드리겠습니다.</p>

<h2 id="21-1-속성-감추기">21-1. 속성 감추기</h2>
<p>```python
class Person:
  def <strong>init</strong>(self, n, a):
    self.__name = n
    self.__age = a
  def add_age(self, a):
    if(a &lt; 0):
      print(‘나이 정보 오류’)
    else:
      self.__age += a</p>

<p>def <strong>str</strong>(self):
    return ‘{0}: {1}’.format(self.__name, self.__age)</p>

<p>def main():
  p = Person(‘James’, 22)
  p.add_age(1)
  # p.__age -= 1 # 이 문장 실행하면 오류 발생된다!</p>
:ET